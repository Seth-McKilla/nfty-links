spring.redis.host=${REDIS_HOST}
spring.redis.password=${REDIS_PASSWORD}
spring.redis.port=${REDIS_PORT}

jwt.secret=${JWT_SECRET:nftlink}

deusExMachina.treasuryAddress=${TREASURY_ADDRESS}
blockchain.url=${BLOCKCHAIN_URL}